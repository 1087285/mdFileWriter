状態：未解決、解決済み

1. mdファイルを読み込んだとき、空白2個が改行として認識しない。mdファイルの形式を全て正しく認識して表示しているか確認が必要。（解決済み）
- **解決日**: 2026-03-04
- **解決内容**: 実装工程（v1.2.0）にて `preprocessMarkdown()` 関数を追加。`loadFile()` 内で `setMarkdown()` 呼び出し前に行末スペース2個以上を正確に2個へ正規化するよう修正。要件定義・基本設計・詳細設計も CommonMark 準拠要件として更新（v1.2.0〜v1.3.0）。
- **解決者**: 09_bug_analysis_agent
<!-- RESOLVED -->
2. markdawn表示の場合は正しく開くことができるが、WYSIWYG表示でmdファイルを読み込むと、空白2個が改行として認識しない。（未解決）
- **ST-E03 Windows実機確認（2026-03-04）**: v1.3.0（`customHTMLRenderer.hardBreak` 対応済み）でもWYSIWYGモードで `<br>` として表示されないことをWindows実機で確認。
- **再現ファイル（抜粋）**:
  ```markdown
  ## 前提条件  
  ・単体評価結果について  
  　ー現在単体評価結果は社内サーバ上にある。  
  　ー関数ごとにフォルダ分けされている。  
  　－フォルダ階層の深さは一定でない。  
  　ーフォルダには単体テスト結果(html)、プリプロセッサ処理後のCソース（拡張子はi）が格納されている。
  ```
- **症状**: 行末スペース2個付き行が複数連続するとき、WYSIWYGモードで改行されず1段落に結合されて表示される。
- **v1.3.0での対応状況**: `customHTMLRenderer.hardBreak` を追加したが効果なし。根本原因の再分析が必要。