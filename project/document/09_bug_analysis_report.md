# 09 不具合混入工程解析レポート

| 版数 | 作成日 | 変更概要 |
|------|------------|----------------------------------------------|
| 1.0.0 | 2026-03-04 | 初版作成。不具合 #2 を解析対象として実施。 |
| 1.1.0 | 2026-03-04 | v1.3.0 修正（customHTMLRenderer.hardBreak）が Windows 実機で効果なしと判明。根本原因・修正案を更新。 |
| 1.2.0 | 2026-03-04 | 不具合 #3（Markdownソースタブ表示崩れ）を追加解析。v1.5.0 バックスラッシュ改行対応の結果を記録。 |

---

## 1. 解析対象不具合一覧

| # | 症状概要 | 状態 | 備考 |
|---|----------|------|------|
| 1 | mdファイルを読み込んだとき、空白2個が改行として認識しない | 解決済み | `<!-- RESOLVED -->` タグ付与対象（後述） |
| 2 | WYSIWYG表示でmdファイルを読み込むと、空白2個が改行として認識しない | **未解決（継続解析中）** | v1.3.0 修正（customHTMLRenderer.hardBreak）適用済みも Windows 実機で効果なし |
| 3 | Markdownソースタブで行の内容が別の行の内容に化ける | **解決済み** | v1.5.0（`preprocessMarkdown` バックスラッシュ改行方式）で修正 |

> 不具合 #1 は `（解決済み）` と記録されているため解析をスキップする。  
> `<!-- RESOLVED -->` タグが欠落しているため、本レポート末尾にて `bug/bugs.md` への追記を行う。

> **v1.1.0 追記**: 不具合 #2 は v1.3.0 での修正試行後も未解決のため継続解析対象とする。

> **v1.2.0 追記**: 不具合 #3 は v1.5.0 にて `preprocessMarkdown` をバックスラッシュ改行（`\` + 改行）方式に変更することで解決。

---

## 2. 不具合解析結果

### 不具合 #2: WYSIWYG表示でのtrailing-space改行非認識

---

#### ▼ v1.0.0 解析結果（初版：2026-03-04）

- **根本原因工程（初版判定）**: 03 - 詳細設計
- **根本原因の説明**（初版）:  
  詳細設計書の `preprocessMarkdown()` 仕様（§4.3.3a）が「trailing-space の正規化」を定めたが、WYSIWYG レンダリングにまで効果を持つかどうかを設計段階で検証・明確化できていなかった。

---

#### ▼ v1.1.0 追加解析（2026-03-04）― v1.3.0 修正後も再現継続

**v1.3.0 修正内容（`customHTMLRenderer.hardBreak` 追加）が機能しない理由**:

Toast UI Editor v3 において `customHTMLRenderer` が作用するのは以下のパイプラインのみである。

| パイプライン | customHTMLRenderer の適用 |
|---|---|
| マークダウン → HTML プレビュー（preview panel） | ✅ 適用される |
| `editor.getHTML()` による HTML 出力 | ✅ 適用される |
| **WYSIWYG モード（ProseMirror 直接 DOM 描画）** | ❌ **適用されない** |

WYSIWYG モードの描画フロー:
1. `setMarkdown(content)` → Toast UI Editor 内部 Markdown パーサーが Markdown AST を生成
2. Markdown AST → **ProseMirror Document** に変換（`toastmark` → ProseMirror converter）
3. ProseMirror が **ProseMirror Schema の `toDOM` 定義に基づいて直接 DOM をレンダリング**
4. **`customHTMLRenderer` はこのパイプラインに介入しない**

そのため、`customHTMLRenderer: { hardBreak() { return [{ type: 'html', content: '<br>' }]; } }` は WYSIWYG 上の改行表示に一切効果を持たない。

**根本原因工程（v1.1.0 更新判定）**: **03 - 詳細設計**（継続）  

v1.3.0 での詳細設計（§4.3.1）に `customHTMLRenderer.hardBreak` を必須仕様として追加したが、この API が「WYSIWYG ProseMirror 描画には作用しない」という Toast UI Editor v3 の API 仕様を考慮せず、誤った修正方針を設計として確定させた。根本原因工程は引き続き **03 詳細設計** である。

**見逃し工程（v1.1.0 更新）**:
- **04 - 実装**: `customHTMLRenderer` の API スコープ（preview/HTML出力のみ）を検証せずに実装した
- **05・06 - テスト**: JSDOM 制約により WYSIWYG 実描画の確認が不可能なまま
- **07 - システムテスト**: ST-E03 Manual:Pending が Pending のままリリースタグを打った

**ST-E03 Windows実機確認（2026-03-04）**:  
v1.3.0 アプリにて以下のファイルを WYSIWYG モードで開いた場合、行末スペース2個付き行が連続する箇所が改行されずに1段落として表示されることを確認。

```markdown
## 前提条件  
・単体評価結果について  
　ー現在単体評価結果は社内サーバ上にある。  
　ー関数ごとにフォルダ分けされている。  
　－フォルダ階層の深さは一定でない。  
　ーフォルダには単体テスト結果(html)、プリプロセッサ処理後のCソース（拡張子はi）が格納されている。
```

- **根本原因工程（確定）**: 03 - 詳細設計
- **見逃し工程**: 04 実装 / 05 単体テスト / 06 結合テスト / 07 システムテスト
- **発見工程**: 07 システムテスト（Windows 実機 ST-E03 Manual 確認）

**証拠（v1.1.0 追加分）**:

| 文書 | 該当箇所 | 内容 |
|---|---|---|
| `03_detailed_design.md` v1.3.0 §4.3.1 | `customHTMLRenderer.hardBreak` 仕様 | WYSIWYG に作用しない API を必須仕様として追加 |
| `04_implementation.md` v1.3.0 §3d | 変更内容 | `customHTMLRenderer.hardBreak` 実装。API スコープ確認の記録なし |
| `07_system_test.md` v1.4.0 ST-E03 | Auto:Pass / Manual:Pending | Windows 実機確認 Pending のまま v1.3.0 タグ打ち |
| `bug/bugs.md` #2 | ST-E03 Windows実機確認（2026-03-04） | v1.3.0 でも WYSIWYG 改行されないことを確認 |
  詳細設計書（v1.2.0）の `preprocessMarkdown()` 仕様（§4.3.3a）において、trailing-space（行末スペース2個）の解決策として「`/ {2,}\n/g` → `'  \n'` で入力を正規化して渡す」方式が定められた。しかしこの方式は、**Toast UI Editor の `setMarkdown()` に渡す文字列中のスペースを保護することしか保証しない**。

  Toast UI Editor の WYSIWYG エンジン（ProseMirror ベース）が、`setMarkdown()` で受け取ったMarkdown文字列を内部でHTML DOMに変換する際、行末スペースを `<br>` としてレンダリングするかどうかは別問題である。実際、多くのWYSIWYGエディタはMarkdown→DOM変換時にProseMirror内部でtrailing-spaceを除去または無視するため、`preprocessMarkdown` で文字列を保護しても WYSIWYG 上の表示には反映されない。

  詳細設計書自身が「実装工程で検証・確定すること」と記述しており（§4.3.3a）、本アプローチが WYSIWYG レンダリングにまで効果を持つかどうかを設計段階で検証・明確化できていなかった。このため、不十分な解決策が確定仕様として設計書に記載されたことが根本原因と判断する。

- **見逃し工程**:
  - **04 - 実装**: `preprocessMarkdown` を実装したが、実際の WYSIWYG 表示での `<br>` レンダリングを手動確認せずにリリース候補とした。詳細設計の "実装工程で検証・確定すること" という指示を遂行しなかった。
  - **05 - 単体テスト**: テストケース IT-MD-001〜004 は「`setMarkdown` に渡される文字列がスペース2個を維持しているか」のみ検証しており、WYSIWYG 上での `<br>` レンダリングは JSDOM の制約により検証不能として除外された。この観点の欠落が見逃しとなった。
  - **06 - 結合テスト**: 同上（IT-MD-001〜004 は文字列の正規化のみを確認）。WYSIWYG 実レンダリングはシステムテストに委ね、テスト設計から除外された。
  - **07 - システムテスト**: ST-E03 の Manual 確認（Pending）が未実施のまま、WYSIWYG 本体の表示動作が検証されなかった。

- **見逃しの理由（共通）**:
  JSDOM 環境では Toast UI Editor の WYSIWYG レンダリングを実動作確認できないため、各テスト工程が「文字列の前処理ロジック検証で代替」という方針をとった。結果として「実際に WYSIWYG 上で改行が見えるか」という最終確認が Windows 実機（Manual）にのみ委ねられ、その Manual テストが未実施のまま不具合として残った。

- **発見工程**: 開発中の手動確認またはユーザー報告（バグ #2 として `bug/bugs.md` に記録）

- **証拠**:
  | 文書 | 該当箇所 | 内容 |
  |------|----------|------|
  | `03_detailed_design.md` v1.2.0 §4.3.3a | `preprocessMarkdown` 仕様 | "実装工程で検証・確定すること" と記載。WYSIWYG レンダリング動作の保証なし |
  | `04_implementation.md` v1.2.0 §3c | 変更内容 | `preprocessMarkdown` の実装は完了。WYSIWYG 実機確認の記録なし |
  | `05_unit_test.md` v1.4.0 IT-MD-001〜004 | テスト結果 | 文字列正規化のみ確認。WYSIWYG レンダリング確認なし |
  | `07_system_test.md` v1.3.0 ST-E03 | システムテスト | "Auto:Pass / **Manual:Pending**" — WYSIWYG 実レンダリング未確認 |
  | `bug/bugs.md` #2 | 不具合記述 | "markdawn表示の場合は正しく開くことができるが、WYSIWYG表示でmdファイルを読み込むと、空白2個が改行として認識しない" |

- **再発防止策**:

  | 工程 | 対策 |
  |------|------|
  | **03 詳細設計** | サードパーティライブラリのレンダリング動作を前提とする仕様を定める際は、「XXXライブラリがYYY仕様を満たすこと」を明示的に検証項目として設計書に記載する。"実装工程で確認" の委任を行う場合は、確認方法・合否基準・確認期限を合わせて明示すること |
  | **04 実装** | 詳細設計から "実装工程で検証・確定すること" が付いた項目は、実装後に必ず動作確認（手動またはE2E）を行い、その結果を実装記録に記述すること。確認できない場合は担当者にエスカレーションし、未確認のままリリース候補にしないこと |
  | **05 単体テスト・06 結合テスト** | JSDOM 制約で確認不能なレンダリング観点については、「未確認」として明示的にトラッキングし、代替確認手段（Playwright・Spectron・スクリーンショット差分等）の適用検討を記録すること。テスト除外理由の記録を必須とすること |
  | **07 システムテスト** | Manual:Pending のテスト項目は、リリース前に完了させることをリリース判定の条件に含めること。Pending が残る場合は「既知のリスク」として記録し、承認者の sign-off を得ること |

**技術的修正案（v1.1.0 更新版 ― Bug #2 解消のための正しい実装変更案）**:

v1.3.0 の `customHTMLRenderer.hardBreak` は WYSIWYG に効果がないため破棄し、以下の修正案を 03 詳細設計工程にて再設計すること。

| 案 | 方法 | 概要 | リスク |
|---|------|------|--------|
| **A** ❌ | `preprocessMarkdown` で `  \n` → `<br>\n` に変換してから `setMarkdown()` に渡す | Toast UI Editor の WYSIWYG モードはインライン HTML を認識し `<br>` を ProseMirror `hard_break` ノードに変換する。`getMarkdown()` でのセーブ時に `hard_break` は `  \n` として復元される | **不具合 #3 の原因となった方式**。v1.4.0 で採用したが `setMarkdown('<br>\n')` → `getMarkdown()` の round-trip でコンテンツ行がずれる問題が発生。v1.5.0 で破棄。 |
| **B ✅ 採用（v1.5.0）** | `preprocessMarkdown` で `  \n` → `\\\n`（バックスラッシュ改行）に変換 | CommonMark の明示的ハードブレーク。HTML パースを経由しないため `getMarkdown()` 往復でコンテンツが崩れない。**不具合 #3 を修正**。Toast UI Editor v3 がバックスラッシュ改行をサポートする場合に WYSIWYG 改行表示も期待できる | Toast UI Editor v3 での WYSIWYG 表示）は Windows 実機（ST-E03 Manual）で確認待ち |
| **C** | `setMarkdown()` 後に `editor.setHTML(editor.getHTML())` でHTMLを再セット | HTML プレビューは `customHTMLRenderer.hardBreak` が効くため `<br>` が含まれる。それを WYSIWYG に再反映させる | 二重変換による情報欠落リスク・パフォーマンス影響あり |

> ⚠️ **v1.5.0 適用結果**: 案 B（バックスラッシュ改行）を v1.5.0 で実装。不具合 #3（Markdown ソースタブ表示崩れ）は解決。WYSIWYG での改行表示（ST-E03 Manual）は Windows 実機確認待ち。

---

## 3. 工程別根本原因件数集計

```
工程別根本原因件数（解析対象：未解決不具合 1件 / v1.1.0 時点）

01 要件定義  | 0件 |
02 基本設計  | 0件 |
03 詳細設計  | ■■■■■ 1件 ← 根本原因確定（v1.0.0・v1.1.0 共通）
04 実装      | 0件 |（見逃し工程：誤った API を使用した修正を確認せず実装）
05 単体テスト| 0件 |（見逃し工程：JSDOM で WYSIWYG 実描画確認不可）
06 結合テスト| 0件 |（見逃し工程：同上）
07 ｼｽﾃﾑﾃｽﾄ  | 0件 |（見逃し工程：ST-E03 Manual Pending のままリリース）
```

```
工程別根本原因件数（v1.2.0 追加解析：不具合 #3 含む）

01 要件定義  | 0件 |
02 基本設計  | 0件 |
03 詳細設計  | ■■■■■ 1件 |（不具合 #2）
04 実装      | ■■■■■ 1件 |（不具合 #3 ← 根本原因: round-trip 未検証のまま <br>\n 方式を実装）
05 単体テスト| 0件 |（見逃し工程：round-trip テストなし）
06 結合テスト| 0件 |（見逃し工程：同上）
07 ｼｽﾃﾑﾃｽﾄ  | 0件 |
```

- **最も不具合が多い工程**: 03 詳細設計（1件）/ 04 実装（1件）
- **見逃し工程として多く関与**: 04〜07 すべての工程（WYSIWYG 実レンダリング確認の連鎖的除外）

---

### 不具合 #3: Markdownソースタブで行の内容が別の行の内容に化ける（v1.5.0 解決済み）

- **根本原因工程**: 04 - 実装
- **根本原因の説明**:  
  v1.4.0 で `preprocessMarkdown()` を `<br>\n` 変換方式に変更した際、`setMarkdown('<br>\n...')` → `getMarkdown()` の round-trip でコンテンツが正しく再構成されるかを検証せずに実装・リリースした。Toast UI Editor は `<br>` インライン HTML を内部パースする過程でパラグラフ境界の解釈が変わり、全角文字を含む行のコンテンツが次行の内容に置き換わって Markdown ソースビュー（`getMarkdown()` 出力）に表示された。
- **見逃し工程**: 05 - 単体テスト / 06 - 結合テスト
- **見逃しの理由**:  
  `integration.test.js` の IT-MD-001〜006 は `setMarkdown` に渡す引数文字列の確認のみを行い、`getMarkdown()` 出力（Markdown ソースビュー表示内容）の正確性を検証していなかった。v1.4.0 導入時に round-trip テストが設計されなかった。
- **発見工程**: ユーザー報告（Windows 実機、Markdown ソースタブ）
- **証拠**:

  | 文書 | 該当箇所 | 内容 |
  |------|----------|------|
  | `04_implementation.md` v1.4.0 §2.3 | `preprocessMarkdown` `<br>\n` 変換方式記述 | round-trip 確認の記録なし |
  | `05_unit_test.md` v1.6.0 IT-MD-001〜006 | `setMarkdown` 呼び出し引数確認 | `getMarkdown()` 出力の正確性を未検証 |
  | `bug/bugs.md` #3 | 不具合記述 | ユーザー報告内容と再現条件 |

- **再発防止策**:

  | 工程 | 対策 |
  |------|------|
  | **04 実装** | エディタ API への入力変換を変更する際は必ず `setMarkdown()` → `getMarkdown()` の round-trip テストを実装前に設計し、round-trip 後のコンテンツが元の内容に近いことを自動テストで確認する |
  | **05/06 テスト** | `preprocessMarkdown` に関するテストケースに、変換後文字列の `setMarkdown` 呼び出し確認だけでなく、エディタの `getMarkdown()` 出力のコンテンツ整合性テスト（round-trip テスト）を追加する |

---

## 4. プロジェクト全体的な品質課題の考察

本プロジェクトにおいて観察された品質課題は以下のとおりである。

1. **サードパーティライブラリへの動作依存の検証不足**  
   Toast UI Editor（ProseMirror ベース）は WYSIWYG モードでの CommonMark の trailing-space 対応について公式ドキュメントに明記されていない可能性が高い。ライブラリ採用時に "CommonMark 準拠" の範囲を明確に調査せず、前処理によるワークアラウンドを設計した結果、根本的な解決に至らなかった。

2. **"後続工程で確認" の連鎖的未実施**  
   詳細設計の "実装工程で検証・確定すること" → 実装での未確認 → 単体テストの JSDOM 制約による除外 → 結合テストの同除外 → システムテストの Pending という連鎖が発生した。各工程で "次の工程に委ねる" が積み重なると、最終確認が永遠に未実施となるリスクがある。各工程で "自工程内で確認できない場合の代替手段または受け渡し条件" を明示するプロセスが必要。

3. **WYSIWYG実動作のテスト環境整備不足**  
   開発環境が Linux Dev Container に固定されており、Windows 実機での GUI テストが困難な状況がある。Playwright Electron を活用したクロスプラットフォームE2Eテスト環境の整備、または CI での Windows ビルド・テスト環境の構築を検討することで、JSDOM で確認できない観点をカバーできる。

---

## 5. bug/bugs.md 更新内容

- 不具合 #1 への `<!-- RESOLVED -->` タグ付与（欠落していたため追加）
- 不具合 #2 は未解決のため現状維持
- 不具合 #3 を追加（v1.4.0 で混入した `<br>\n` 変換によるソースタブ表示崩れ）、v1.5.0 修正内容を記録し `<!-- RESOLVED -->` タグを付与

---

*本レポートは 09 不具合混入工程解析エージェント により 2026-03-04 に生成（v1.0.0）、v1.3.0 修正失敗受けて同日 v1.1.0 に更新、v1.5.0 バックスラッシュ改行対応実施を受けて同日 v1.2.0 に更新。*
